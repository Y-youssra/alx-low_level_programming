#include <stdio>
/**
 * main - A program that print a line using the printf fonction
 * Return: 0 (success) 
 */

int main(void)
{
	Printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}
